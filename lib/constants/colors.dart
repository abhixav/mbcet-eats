import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF00695C);          // Teal green
  static const background = Color(0xFFF1F1F1);       // Default background
  static const backgroundGrey = Color(0xFFF7F7F7);   // Soft light grey for card or screen backgrounds
  static const accent = Color(0xFFFF7043);           // Orange accent
  static const text = Color(0xFF212121);             // Almost black text
}
